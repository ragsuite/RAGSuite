# Http-context map (loaded via conf.d before default.conf).
# auth_request_set can copy an empty X-Embed-CSP; never let that clear fail-open *.
map $embed_csp_upstream $embed_csp {
    ""      "frame-ancestors *";
    default $embed_csp_upstream;
}
