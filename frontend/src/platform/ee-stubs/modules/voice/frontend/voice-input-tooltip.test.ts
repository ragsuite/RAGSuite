/**
 * CE stub — real coverage lives in RAGSUITE_EE `modules/voice`.
 */
describe('voice-input-tooltip (CE stub)', () => {
  it('does not run EE tooltip placement tests without RAGSUITE_EE', () => {
    expect(true).toBe(true);
  });
});
