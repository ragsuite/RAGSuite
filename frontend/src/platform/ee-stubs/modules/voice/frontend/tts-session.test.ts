/**
 * CE stub — real coverage lives in RAGSUITE_EE `modules/voice`.
 */
describe('tts-session (CE stub)', () => {
  it('does not run EE tts-session tests without RAGSUITE_EE', () => {
    expect(true).toBe(true);
  });
});
