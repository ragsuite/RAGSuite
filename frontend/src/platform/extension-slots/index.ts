export { ExtensionSlot } from '@/platform/extension-slots/ExtensionSlot';
export {
  getExtensionSlot,
  registerExtensionSlot,
  resetExtensionSlots,
} from '@/platform/extension-slots/registry';
export type {
  ExtensionSlotId,
  ExtensionSlotPropsMap,
  VoiceInputSlotProps,
  VoiceOutputSlotProps,
} from '@/platform/extension-slots/types';
